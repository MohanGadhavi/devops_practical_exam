pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }
        stage('Build and Run') {
            steps {
                sh 'python index.py'
            }
        }
        stage('Test') {
            steps {
                sh 'Job Tested successfully'
            }
        }
    }
}
